<%= form_with(model: hub, url: hub_path(hub), local: true, method: :patch) do |f| %>

  <%= f.label :hub_number, class: "label-#{yield(:class_text)}" %>
  <%= f.text_field :hub_number, class: "form-control" %>

  <%= f.label :name, class: "label-#{yield(:class_text)}" %>
  <%= f.text_field :name, class: "form-control" %>

  <%= f.label :attendance, class: "label-#{yield(:class_text)}" %>
  <%= f.text_field :attendance, :placeholder => "出勤または退勤を入力する", class: "form-control" %>

  <%= f.submit yield(:button_text), class: "btn btn-primary btn-block btn-#{yield(:class_text)}" %>
<% end %>
